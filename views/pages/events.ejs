<div class="jumbotron text-center">
    <h1>All Events</h1>
</div>

<% if (success.length > 0) { %>
    <div class="alert alert-success"><%= success %></div>
<% } %>

<table class="table table-bordered table-hover table-striped">
    <thead>
        <tr>
            <th>Event Name</th>
            <th>Description</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        <% for (var event of events) { %>
            <tr>
            <td><%= event.name %></td>
            <td><%= event.description %></td>
            <td><a href="/events/<%= event.slug %>" class="btn btn-small btn-primary">view</a>
                <a href="/events/<%= event.slug %>/edit" class="btn btn-small btn-primary">edit</a>
                <a href="/events/<%= event.slug %>/delete" class="btn btn-small btn-danger">delete</a>
            </td>
        </tr>
        <% } %>

    </tbody>
</table>